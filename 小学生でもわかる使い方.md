# DiscordRecorder 使いかた — 小学生でもわかる！

> むずかしい言葉は使いません。1つずつ、ゆっくりやろう！

---

## 0. これは何？

Discordで通話したとき、
**「相手の声だけ」** を残すアプリだよ。

- あなたは小さな声（ささやき）で話してもOK
- 相手が小さな声でも、ちゃんと拾って大きくするよ
- あとから何回でもやり直せるよ

---

## 1. 用意するもの

1. **Androidのスマホ** (Android 10より新しいもの)
2. **イヤホン** (あった方がきれいに分かれるよ)
3. **パソコン** (WindowsでOK)
4. **USBケーブル** か **ギガファイル便などのクラウド**

---

## 2. アプリを入れる（1回だけ）

### ★ 一番かんたんな方法：APKをもらって入れる（Android Studioいらない！）

> **Android Studioは入れなくてOK！** 難しい作業は大人に1回だけやってもらえばいいよ。

**【大人にお願いする文】** これをそのまま見せよう：
```
「DiscordRecorder/app をビルドして app-debug.apk を作って送ってください。
  パソコンで ./gradlew assembleDebug でできます」
```

もらった `app-debug.apk` をスマホに送ってもらったら（LINEやメール、USBでOK）、次の手順で入れるよ。

### スマホで入れる手順（絵で説明）

**1. `app-debug.apk` をタップ**

ファイルアプリやLINEで受け取った `app-debug.apk` をタップするよ。

```
┌─────────────────────┐
│  app-debug.apk      │
│  [タップ！]          │
└─────────────────────┘
         ↓
┌─────────────────────┐
│ このアプリをインストールしますか？ │
│  [インストール] を押す      │
└─────────────────────┘
```

**2. 「ブロックされました」と出たら？**

```
┌──────────────────────────────┐
│ 不明なアプリのインストールはブロックされました │
│  [設定] を押す                      │
└──────────────────────────────┘
         ↓
┌──────────────────────────────┐
│ この提供元を許可  ○ OFF → ● ON にする  │
│  スイッチを右にスライド！            │
└──────────────────────────────┘
         ↓
「←」で戻る → もう一度 `app-debug.apk` をタップ → 今度は `インストール` が出る！
```

**3. 「インストール」を押して「開く」を押す**

```
┌─────────────────────┐
│ インストール中...    │
│  ████████ 100%      │
│  [開く] を押す       │
└─────────────────────┘
```

これで完了！ホーム画面に `DiscordRecorder` のアイコンが出るよ。

> **よくある「わからない」**
> - Q. `app-debug.apk` が見つからない → 大人に「apk送って」ともう一度お願いしよう。届いたら `ダウンロード` フォルダにあるよ
> - Q. 「許可」が出ない → 設定 > アプリ > 特別なアクセス > 不明なアプリのインストール > 使ったアプリ（ChromeやLINE）を `許可` にする
> - Q. Android Studioって何？ → **今は無視してOK！** APKさえもらえればStudioは一生使わないよ

### 自分で作りたい人向け（上級者）

パソコンに `DiscordRecorder.zip` を解凍して、フォルダの中でこれを打つだけ。Studio不要！

```
./gradlew assembleDebug
```

終わると `app/build/outputs/apk/debug/app-debug.apk` ができるよ。これをスマホに送る。

---

## 3. 録音する（スマホで）

### 手順は3タップだけ！

**1. アプリを開く**

画面に `DiscordRecorder` と出るよ。

**2. 「録音開始」を押す**

3つの許可が出るよ。全部「許可」を押そう！

- 「マイクの許可」→ 許可
- 「通知の許可」→ 許可
- 「画面の録画を許可しますか？」→ **必ず「今すぐ開始」を押す！** これが一番大事！

**3. Discordで通話する**

いつも通り通話してOK。イヤホンをつけよう。
画面の上に `● 録音中...` と出ていれば成功！

**4. 終わったら「停止」を押す**

アプリの `停止` ボタン **か** 上の通知の `停止` を押す。

**5. 保存場所を確認**

スマホの `ファイル` アプリを開く → `Music` → `DiscordRecorder` → `20260824_120000` のような名前のフォルダ

中に3つあるか見よう！

- `playback.wav` … 相手の声が入ってる
- `mic.wav` … 自分の声が入ってる
- `meta.json` … いつ録ったかのメモ

これでスマホ側は終わり！

---

## 4. パソコンに移す

**一番かんたんな方法：USBでコピー**

1. スマホとパソコンをUSBケーブルでつなぐ
2. スマホで「ファイル転送」を選ぶ
3. パソコンで `PC > スマホ > Music > DiscordRecorder` を開く
4. `20260824_120000` フォルダを、パソコンの `ダウンロード` などにコピー

> USBがなければ、Googleドライブにアップロード → パソコンでダウンロードでもOK！

---

## 5. 相手の声だけにする（パソコンで）

### 初回だけ：準備（1回で終わり）

パソコンで `コマンドプロンプト` または `PowerShell` を開いて、これをコピペしてEnter！

```
python -m venv venv
.\venv\Scripts\activate
pip install soundfile librosa numpy
```

終わったら準備完了！

### 毎回やること：1行コピペ！

`DiscordRecorder` フォルダの中の `scripts` まで移動して、

```
python separate_speakers.py --playback C:\ダウンロード\20260824_120000\playback.wav --mic C:\ダウンロード\20260824_120000\mic.wav --output C:\ダウンロード\できたよ
```

と打ってEnter！

※ `C:\ダウンロード\...` の部分は、さっきコピーした場所に直してね。エクスプローラーでファイルを右クリック → 「パスのコピー」で取れるよ。

終わると `C:\ダウンロード\できたよ` の中に

- `other_only.wav` … **相手の声だけ！** これが欲しかったもの！
- `mic_only.wav` … 自分の声だけ（確認用）
- `segments.json` … どこで誰が喋ったかのメモ

ができるよ。ダブルクリックで聞いてみよう！

---

## 6. うまくいかないとき

| こまった！ | やってみよう |
|---|---|
| **相手の小さい声が消える** | ` --play-thresh-db -52 --max-gain-db 24` を最後に付け足して、もう一回実行！ |
| **自分のささやきが残る** | ` --mic-thresh-db -52 --mask-pad-ms 0.35` を付け足して、もう一回実行！ |
| **もっとキレイにしたい** | ` --use-silero` を付け足すと、AIが賢く分けてくれるよ（初回はネットが必要） |
| **録音が保存されてない** | 「画面の録画を許可」を押し忘れてない？ もう一度「録音開始」からやり直そう |
| **音が割れる** | イヤホンをつけて録ろう。スピーカーだと声が混ざっちゃうよ |

> **ポイント：録り直しはいらない！** データは生のまま残ってるから、何回でも数字を変えてやり直せるよ。

---

## 7. お片付け

- 聞けたら `other_only.wav` を好きな場所に保存しよう
- いらなくなった `playback.wav` は消してもOK
- 次に録るときは、また「3. 録音する」から始めればOK！

---

## 8. まとめ（大人向けに一言で）

```
[スマホ] 録音開始 → Discord通話 → 停止 → Music/DiscordRecorder/<session>/
   ↓ コピー
[PC] python separate_speakers.py --playback playback.wav --mic mic.wav --output out/
   ↓
[結果] out/other_only.wav が相手のみ
```

わからなくなったら、このファイルの `3. 録音する` に戻って、1つずつやってみてね。できたよ！
